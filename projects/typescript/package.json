{
  "name": "@neonretrorewind/typescript",
  "private": true,
  "license": "Apache-2.0",
  "scripts": {
    "build": "pnpm --recursive --if-present build",
    "check": "pnpm --recursive --if-present check",
    "film-catalog": "node packages/data-compiler/src/cli.ts film-catalog",
    "test": "pnpm --recursive --if-present test"
  },
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "11.x",
      "onFail": "download"
    },
    "runtime": {
      "name": "node",
      "version": "24.19.0",
      "onFail": "download"
    }
  },
  "devDependencies": {
    "@types/node": "24.13.3",
    "typescript": "7.0.2"
  }
}
